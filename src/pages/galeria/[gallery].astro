---
export function getStaticPaths() {
  return [
    {params: {gallery: 'kuchyne'}},
    {params: {gallery: 'obyvacieSteny'}},
    {params: {gallery: 'vstavaneSkrine'}},
    {params: {gallery: 'spalne'}},
    {params: {gallery: 'kupelne'}},
    {params: {gallery: 'prislusenstvo'}},
  ];
}

import Layout from '../../layouts/Layout.astro';
import Gallery from '../../components/Gallery';
import GalleryNavbar from '../../components/GalleryNavbar';
import HeaderName from '../../components/HeaderName';

const { gallery } = Astro.params;
---

<Layout title={`Galéria`}>
    <div class="absolute h-[800px] w-[1100px] rounded-bl-full right-0 bg-red-200/30">
    </div>
      <GalleryNavbar />
    <div class='relative justify-center items-center flex'>
      <HeaderName gallery={gallery} />
    </div>
      <Gallery client:load gallery={gallery}/>
</Layout>
